---
title: "线性回归：原理与实现"
date: 2026-02-10
draft: false
description: "线性回归算法的数学原理、推导过程与代码实现"
tags: ["机器学习", "回归", "线性回归"]
categories: ["机器学习"]
series: ["回归算法"]
math: true
---

## 📋 概述

线性回归是机器学习中最基础也是最重要的算法之一。它通过拟合一条直线（或超平面）来建立特征与目标值之间的线性关系，广泛应用于预测和分析任务。

（您可以在这里添加更多引言内容...）

---

## 一、什么是线性回归

**🎯 目的**：
- 找到特征 $X$ 和目标值 $y$ 之间的线性关系
- 对新数据进行预测
- 理解特征对目标值的影响程度

**🛠️ 基本形式**：

**一元线性回归**（单特征）：
$$
y = \beta_0 + \beta_1 x + \epsilon
$$

**多元线性回归**（多特征）：
$$
y = \beta_0 + \beta_1 x_1 + \beta_2 x_2 + \cdots + \beta_n x_n + \epsilon
$$

向量形式：
$$
y = \mathbf{w}^T \mathbf{x} + b
$$

其中：
- $y$：目标值（因变量）
- $x_i$：特征（自变量）
- $\beta_i / w_i$：权重系数
- $\beta_0 / b$：偏置项（截距）
- $\epsilon$：误差项

**💡 核心思想**：找到最优的权重 $\mathbf{w}$ 和偏置 $b$，使得预测值 $\hat{y}$ 与真实值 $y$ 的差距最小。

---

## 二、数学原理

### 2.1 模型表示

（您可以在这里补充详细推导...）

### 2.2 损失函数

**均方误差（MSE）**：
$$
J(\mathbf{w}) = \frac{1}{2m} \sum_{i=1}^{m} (h(\mathbf{x}_i) - y_i)^2
$$

### 2.3 求解方法

两种主要方法：
1. **梯度下降法**（迭代优化）
2. **正规方程法**（解析解）

---

## 三、梯度下降法

### 3.1 批量梯度下降

更新规则：
$$
\mathbf{w} := \mathbf{w} - \alpha \nabla J(\mathbf{w})
$$

### 3.2 随机梯度下降

（补充 SGD 细节...）

### 3.3 小批量梯度下降

（补充 Mini-batch 细节...）

---

## 四、正规方程法

正规方程解：
$$
\mathbf{w}^* = (\mathbf{X}^T \mathbf{X})^{-1} \mathbf{X}^T \mathbf{y}
$$

---

## 五、代码实现

### 5.1 使用 NumPy 实现

```python
import numpy as np

class LinearRegression:
    def __init__(self, learning_rate=0.01, n_iterations=1000):
        self.lr = learning_rate
        self.n_iterations = n_iterations
        self.weights = None
        self.bias = None
    
    def fit(self, X, y):
        """训练模型"""
        n_samples, n_features = X.shape
        self.weights = np.zeros(n_features)
        self.bias = 0
        
        for _ in range(self.n_iterations):
            y_pred = np.dot(X, self.weights) + self.bias
            dw = (1/n_samples) * np.dot(X.T, (y_pred - y))
            db = (1/n_samples) * np.sum(y_pred - y)
            self.weights -= self.lr * dw
            self.bias -= self.lr * db
    
    def predict(self, X):
        return np.dot(X, self.weights) + self.bias
```

### 5.2 使用 Scikit-learn

```python
from sklearn.linear_model import LinearRegression

model = LinearRegression()
model.fit(X_train, y_train)
predictions = model.predict(X_test)
```

---

## 六、模型评估

### 6.1 评估指标

- **MSE**：均方误差
- **RMSE**：均方根误差
- **R² Score**：决定系数

### 6.2 过拟合与欠拟合

（补充解决方案...）

---

## 七、线性回归的扩展

### 7.1 多项式回归

（补充多项式回归细节...）

### 7.2 正则化（Ridge & Lasso）

**Ridge 回归**（L2）：
$$
J(\mathbf{w}) = \text{MSE} + \lambda \sum_{j=1}^{n} w_j^2
$$

**Lasso 回归**（L1）：
$$
J(\mathbf{w}) = \text{MSE} + \lambda \sum_{j=1}^{n} |w_j|
$$

---

## 💡 总结

（您的总结...）

---

## 📚 参考资料

1. 《统计学习方法》- 李航
2. 《机器学习》- 周志华
3. Scikit-learn 官方文档
